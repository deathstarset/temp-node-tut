/* 
  Global variables are vriables that can be accessed in any place in the application
  Some important global variables : 
    -- __dirname => gives you the path to the current directory 
    -- __filename => gives you to the current file 
    -- require => function that allows you to use modules (CommonJs)
    -- module => give you info about the current module (file)
    - process => gives you infos about the environment where the program is being executed
*/
// example
console.log(__filename);
